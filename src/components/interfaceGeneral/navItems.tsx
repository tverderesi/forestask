import React from 'react';
import DarkMode from '../buttons/DarkMode';
import Tasks from '../buttons/Tasks';
import TeacherMode from '../buttons/TeacherMode';

export const navItems = [<DarkMode />, <Tasks />, <TeacherMode />];
